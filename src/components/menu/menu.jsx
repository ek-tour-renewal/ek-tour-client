import React from 'react';
import Button from '../button/button';
import styles from './menu.module.css';

const Menu = (props) => {
  return (
    <section>
      <h1>menu</h1>
      <Button />
    </section>
  )
};

export default Menu;